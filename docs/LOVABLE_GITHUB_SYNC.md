# Lovable GitHub sync

**Latest ship:** `6f7cabd1` — fix(vite): enoent guard skip SPA routes — fixes Lovable /auth 500

**GitHub branches (must match):**
- `main` → `6f7cabd1`
- `feature/service-library-nav` → `6f7cabd1` ← Lovable watches this branch

**Repo:** `WebdeveloperFLC/flow-link-docs`

If Lovable → Sync shows “nothing to update”, the code is **already in the Lovable editor**. Click **Publish** to rebuild preview.

Verify in Lovable code view: `vite.config.ts` should contain `projectRoot` guard (skip SPA routes like `/auth`).
