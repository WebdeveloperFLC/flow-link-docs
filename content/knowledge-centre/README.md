# Knowledge Centre content (archived)

**Knowledge Centre v1.0** stores all guide content in **`content/service-library/*.json`**, uploaded to `service_library.academy_metadata`.

This folder is not used for active imports. See:

- `docs/KNOWLEDGE_CENTRE_JSON_SPECIFICATION.md` — frozen JSON contract
- `content/service-library/canada-student-visa.json` — production reference
- `docs/knowledge-centre/` — narrative gold standard and phase-1 archive docs
- `archive/knowledge-centre-phase1/` — retired standalone KC module

Do not add import JSON here.
