## Plan — Post May 2026 Office Rent Journal

The database has no entity for FLVC and no usable COA accounts, so I'll seed the minimum needed, then post the entry. All work is data-only (no schema changes, no code edits).

### Step 1 — Seed entity

Insert into `accounting_entities`:
- `name`: Future Link Visa Consultants Pvt Ltd
- `type`: COMPANY
- `country`: IN
- `currency`: INR
- `fiscal_year_start`: 04-01
- `is_active`: true

(`id` auto-generated; captured for use below.)

### Step 2 — Seed two COA accounts

Insert into `accounting_coa` (scoped to the new entity, INR):

| code | name         | group_code | type_code      | normal_balance | currency |
|------|--------------|------------|----------------|----------------|----------|
| 6100 | Rent Expense | EXPENSE    | OPERATING_EXP  | DEBIT          | INR      |
| 1110 | HDFC Bank    | ASSET      | BANK           | CREDIT*        | INR      |

*Bank accounts are debit-normal; storing CREDIT here is wrong — I'll use `DEBIT` for HDFC Bank (assets are debit-normal). Setting it explicitly to keep the COA correct.

`entity_id` will be set to the new entity's UUID as text (column is `text`).

### Step 3 — Post the journal

Insert into `accounting_journals`:
- `entry_date`: 2026-05-01
- `entity`: <FLVC entity UUID as text>
- `currency`: INR
- `source_type`: MANUAL
- `reference`: RENT-MAY-2026
- `narration`: Office rent payment for May 2026 — Atlantis Complex, Vadodara
- `status`: POSTED
- `total_debit`: 50000.00, `total_credit`: 50000.00, `is_balanced`: true
- `journal_number`: auto-generated by `generate_journal_number()` trigger

Insert two rows into `accounting_journal_lines`:
1. line 1 — Rent Expense, debit 50000.00, "May 2026 rent"
2. line 2 — HDFC Bank,    credit 50000.00, "Paid via bank transfer"

### Step 4 — Verify

- Read back the journal with its lines.
- Confirm `journal_number` was assigned (e.g. `JE-2026-0001`) and totals balance.
- Report the journal number to you.

### Notes / caveats

- The journal header's `entity` column is plain text; the app's UI selector stores the entity UUID there, so I'm using the UUID. If you prefer the literal company name string instead, say the word and I'll switch.
- COA codes 6100 and 1110 are conventional defaults; tell me if you have a numbering scheme you'd rather follow.
- `created_by` / `posted_by` will be left NULL since the insert runs without an auth session. Journal will still display correctly in the UI.
- No code, no migrations, no RLS changes, no UI touched. Personal Wealth, Commission, and other modules are untouched.
