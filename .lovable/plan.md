## Plan

1. Reset local repo to match origin/main commit d699c276 (already current — no diff detected).
2. Redeploy only the `whatsapp-webhook` Supabase edge function.

No file changes, SQL, or secret updates required.