# Messages

`/messages` is a unified inbox of all chats.

Two channel types per client:
- **Staff internal** — private chat among staff about a client.
- **Staff ↔ Client** — external chat visible to the client via the portal.

Also openable from client detail → Chat tab. New messages trigger notifications (bell icon). Admins see all conversations; counselors see only conversations for clients they're assigned to.
